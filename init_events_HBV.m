function events = init_events_HBV(initial_state,params)
% events = init_events_pertussis(initial_state,params)is used to determine
% whether a specified condition was met during the simulation, or at what
% time some threshold was met

events = [];

end

